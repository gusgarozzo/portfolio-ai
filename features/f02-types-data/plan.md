# F02 — Implementation Plan

## Steps
1. Create `types/portfolio.ts` with all global interfaces
2. Create `data/personal.ts` and export `personal` constant
3. Create `data/summary.ts` and export `summary` constant
4. Create `data/experience.ts` and export `experience` array
5. Create `data/projects.ts` and export `projects` array
6. Create `data/skills.ts` and export `skills` object
7. Create `data/certifications.ts` and export `certifications` array
8. Create `data/education.ts` and export `education` array
9. Remove `data/DATA.md`
10. Verify TypeScript compiles without errors
