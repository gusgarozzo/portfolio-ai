# F05 — Implementation Plan

## Steps
1. Create `components/hero/Hero.tsx` with terminal-inspired header, personal info, and links
2. Create `components/experience/Experience.tsx` with timeline of role Cards, each with highlights as bullet list
3. Create `components/projects/Projects.tsx` with project Cards, subproject metrics as StatusChips
4. Create `components/skills/Skills.tsx` with skill category Cards and Badge items
5. Create `components/certifications/Certifications.tsx` with table/grid layout
6. Create `components/contact/Contact.tsx` with contact info and action buttons
7. Verify all sections look good on 375px and 1280px
8. Run lint and build
