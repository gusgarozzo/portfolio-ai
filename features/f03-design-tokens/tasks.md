# F03 — Task Checklist

- [x] Add all DESIGN.md colors to `@theme inline` in `globals.css`
- [x] Configure `fontFamily` for Inter and JetBrains Mono
- [x] Configure custom typography scale (headline-xl → label-caps)
- [x] Add `glow` boxShadow token
- [x] Set default borderRadius to 0px
- [x] Add `.micro-border` utility in `globals.css`
- [x] Add `.grid-overlay` utility in `globals.css`
- [x] Add `.bloom-cyan` utility in `globals.css`
- [x] Verify build passes with `npm run build`
