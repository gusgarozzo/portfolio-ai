# F06 — Implementation Plan

## Steps
1. Update `app/layout.tsx` with font configuration, metadata, and body structure
2. Update `app/globals.css` with base dark theme, scrollbar, grid overlay
3. Create `app/page.tsx` composing all sections in order
4. Create `components/layout/Navbar.tsx` (optional) with smooth-scroll navigation
5. Create `components/layout/Footer.tsx` (optional) with terminal-inspired system status line
6. Verify all sections render in correct order
7. Verify responsive behavior and build
